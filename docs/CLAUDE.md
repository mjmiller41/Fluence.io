# CLAUDE.md moved to the repo root

Claude Code auto-loads `CLAUDE.md` from the **repository root**, so the canonical
project-memory file now lives at [`../CLAUDE.md`](../CLAUDE.md). Edit that one.
This stub only exists so links to `docs/CLAUDE.md` still resolve.
